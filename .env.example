# Supabase — servidor (seed script / CLI; NUNCA expor no cliente)
DATABASE_URL=
DIRECT_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ACCESS_TOKEN=

# Supabase — cliente (Vite só expõe variáveis com prefixo VITE_)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# YouTube Data API v3 (key restrita por HTTP referrer; vai para o bundle)
VITE_YOUTUBE_API_KEY=
